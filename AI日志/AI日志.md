# C2 AI 协作日志（AI for Math 论文）

- 挑战：C2 AI for Math 论文（ch-20260717031343-8ot0ji）
- 作者：周同学
- 记录周期：2026-09-15 ~ 2026-09-16
- 日志性质：每日 AI 协作记录。凡标注"AI 生成"的结论，均在"人工核验"一列写明核验方式与结论；未通过核验的内容不进入论文。

---

## 一、协作总览

| 阶段 | AI 角色 | 人工角色 | 产物 |
|---|---|---|---|
| 环境 | 建议方案、给命令 | 执行、排错、判定可用性 | tectonic 0.17.0 本地编译链 |
| 选题 | 归纳讲义大纲、给候选方向 | 拍板题目与贡献边界 | `选题与问题定义.md` |
| 检索 | 生成核验脚本、批量查库 | 定纳排标准、抽查命中项 | `lit/refs.json`、`lit/参考文献核验记录.md` |
| 写作 | 起草段落、生成图表公式 | 逐句审读、改主张、删改引用 | `parts/*.tex`、`paper.tex` |
| 核验 | 列出引用—主张清单 | 逐条比对、判定错配并修正 | 31 条引用零错配终态 |
| 排版 | 排错、给编译参数 | 复跑编译、确认 0 error | `paper.pdf`（19 页） |

---

## 二、逐日记录

### 2026-09-15 · 环境与选题

**任务**：让 LaTeX 能编译中文论文；确定选题。

**AI 协作**：AI 先提议"装 MacTeX/brew 装 texlive"，实测本机无 `pdflatex` / `xelatex` / `tectonic`，且 `brew` 也不存在 → 该方案作废。AI 改提"下载 tectonic 独立二进制（自带宏包与字体，无需包管理器）"。实测下载 `tectonic-0.17.0-aarch64-apple-darwin.tar.gz`（21704674 B）到 `/Users/xiaowo/.cache/c2latex/`，二进制 53998928 B，版本 `Tectonic 0.17.0`。

**人工核验**：
- 英文最小样例编译成功（`_smoke/min.pdf` = 32598 B）；
- 中文最小样例初版失败（缺中文字体），改用 `\documentclass[11pt,fontset=fandol]{ctexart}` 后成功（`_smoke/min-zh-fandol.pdf` = 29857 B）。
- 结论：编译链可用，论文正文用 ctexart + fandol 字体集。

**AI 判断的边界**：AI 无法在本机做视觉检查（无图像识别输入），因此"PDF 好不好看"不能由 AI 断言，只能由结构探针 + 人工确认。

### 2026-09-16 · 文献检索与真伪核验（本次最关键的一段）

**任务**：满足"≥8 篇真实可查一手文献"且不触"引用造假"红线。

**AI 协作**：AI 没有直接凭记忆列参考文献，而是写了一个可复跑的核验脚本 `pipeline/verify_refs.py`：
- 用 arXiv API（`export.arxiv.org/api/query?id_list=`，按 ID 精确查询）与 Crossref API（`api.crossref.org/works?query.bibliographic=`）双向核验；
- **只有命中者才进入 `references.bib`**，未命中进入"未能核验"清单；
- 产出三份可审计文件：`lit/refs.json`（结构化结果）、`lit/参考文献核验记录.md`（人读版）、`lit/references-verified.bib`（仅通过核验的条目）。
- 终态：31 条全部核验通过，未核验 0 条。

**人工核验与纠错（AI 出错并被抓出的地方）**：
1. **脚本崩溃**：生成报告时用 `"% 生成时间：%s" % ...`，中文"生"字紧跟在 `%` 之后被当成格式符，抛 `ValueError: unsupported format character '?' (0x751f)`。→ 改为字符串拼接 `"% 生成时间：" + ...`，脚本复跑通过。
2. **核验阈值过松**：Crossref 标题模糊匹配初版把相似度 0.75–0.80 的结果也算命中，抽查发现是"标题像但不是同一篇"。→ 收紧为只信 `sim=1.00` 的精确命中，宁缺毋滥。
3. **arXiv 编号写错**：一条记录的 arXiv 号一度写成 2205.12950（与真实论文无关），核对 `id_list` 返回后改为正确编号。

**AI 结论人工核验原则（本次执行）**：AI 给的每一条"这篇文献说了 X"，都要回到条目标题/出处确认 X 与之匹配；不能只确认"文献存在"就放行。

### 2026-09-16 · 写作与"引用—主张"逐条核验

**任务**：把框架写成 9 节论文，并让每一条引用都真的支撑它所在的那句话。

**AI 协作**：AI 负责起草段落并给出引用位置；人工负责质疑"这句引用放这儿对不对"。

**核验方法**：把 `references.bib` 的 31 个条目导出为"bib key → 标题/年份/出处"清单，逐条与正文语境对照。结果抓出 **4 处错配**，全部修正：

| # | 引用 | AI 原本的主张 | 条目的真实内容 | 处理 |
|---|---|---|---|---|
| 1 | `thakur2023context` | "把新问题的解法检索复用于类似问题" | *An In-Context Learning Agent for Formal Theorem-Proving*（上下文学习定理证明智能体） | 删除该处引用；改放到"把定理证明组织为上下文学习智能体的交互过程"一句 |
| 2 | `ni2023evaluating` | "数据污染会抬高分数" | *Evaluating the Robustness to Instructions of LLMs*（指令鲁棒性） | 拆开：污染句只留 `sainz2023nlp`，`ni2023evaluating` 独立支持"指令措辞变化会影响表现" |
| 3 | `zhou2023solving` | "把复杂问题显式分解并按难度排序求解" | *…GPT-4 Code Interpreter with Code-based Self-Verification*（代码解释器 + 代码级自验证） | 改写为"把推理与代码执行、结果自核验结合"；讨论节中从"生成式推理路线"移入"工具增强路线" |
| 4 | `zhou2024don` | 全库有一条条目从未被正文引用 | *Don't Trust: Verify*（自动形式化落地） | 补引到自动形式化句，消除"未引用条目" |

**这次犯错的类型**：第 3 条是典型的**"凭语义记忆配引用"**——一看到"分解并按难度排序"就想到 least-to-most，于是顺手挂上了一个 key 里带 2023 的 Zhou 条目，但条目其实是另一篇。AI 的语义联想是对的，**连接错了**。对策已固化进流程：引用核验不看"关键词像不像"，只看"导出的标题清单里，这一条的标题是否字面支持这句话"。

### 2026-09-16 · 编译排错与终态验收

**AI 协作**：AI 负责读日志定位错误，人工负责复跑确认。

- 错误 1：`Illegal, another \bibstyle command---line 136 of file paper.aux` → `paper.tex` 里 `\bibliographystyle` 重复设置，删掉多余一处。
- 错误 2（首轮）：首次编译产出的 `paper.xdv` 只有 6 页且中断 → 定位为 `.aux` 残留的历史 `\bibstyle` 冲突，清理后重编通过。
- 终态：`paper.pdf` = 613347 B / **16 页**（第一轮当时值；第三轮重编后为 19 页 / 697674 B），`paper.bbl` 含 **31 条 bibitem**，脚本核验"未被引用的条目 = 空"、"被引但库中缺失 = 空"，日志中仅剩 1 条字体形状警告（`TU/FandolSong-Regular(0)/b/it undefined`，非致命，中文粗斜体回退）。

**必须写明的核验局限（诚实记录）**：
1. 本机 AI 无视觉输入能力，**PDF 版式无法由 AI 肉眼确认**。替代做法是用 PyMuPDF 做结构探针：逐页统计矢量图形/图像数量、文本包围盒是否越界、图/表标题落在第几页。探针显示 图1→p5、表1→p6、图2→p7、表2→p7、表3→p9，文本未越界。（**第一轮口径**：第三轮新增第 8 节后序号整体后移，这组序号定位已作废，现改用标签名重登记，见本日志第三轮记录。）**"没发现问题"不等于"排版没问题"**。
2. 存在 2 处 Underfull \hbox（`parts/05-failure:24`、`parts/06-metrics:21`），属排版松散警告，不影响可编译性与可读性，未强行修改（避免为消警告破坏语义）。
3. 论文未做真实系统实验，指标为定义与可计算性论证，不构成规模经验主张——此边界已在正文与 AAR 中显式声明。

---

## 三、AI 使用原则小结

1. **不让 AI 直接产出参考文献表**：所有条目必须过一遍 arXiv / Crossref 的机器核验，命中才入库。
2. **不让"文献存在"冒充"文献支持"**：引用核验比的是标题与主张的字面匹配。
3. **AI 的自我判断不作为验收依据**：编译是否通过、引用是否闭环，一律用命令与脚本的真实输出判定。
4. **AI 的能力边界写入交付物**：无视觉、无实验条件这两条局限，宁可写进日志与 AAR，也不装作已经验证过。

---

## 四、第二轮提交前的复核（2026-09-23）

距首次提交（2026-09-16）一周后，为第二轮提交做复核。本轮记录三件事。

**1. 复核发现：平台首次返回的 C2 初评分不可用，且不能靠字段识别。**

平台返回的 AI 初评 `eval-20260916091328-bf15cc02` 分数为 76，但其评语原文是「系统兜底初评，非真实 AI 评分……系统在缺少 AI API Key 时生成的确定性初评草稿」。结论：该分数只是占位草稿，**不能当成成绩**。

识别方法不是看分数，而是**回读评语原文**——结构化字段里分不出真伪。对照 C1 的初评（92 分，评语有实质内容、无兜底字样）：同一个分数字段，一个为真、一个为占位。据此固化为规则：**任何带「系统兜底初评／非真实 AI 评分」字样的分数一律判无效**，不得作为成绩汇报。

**2. 复核结论：交付物无需改动，本轮不动正文。**

逐项复核结果——四类必需交付物（`paper.tex`、`references.bib`、`AI日志/AI日志.md`、`七维AAR复盘.md`）齐全；当时工作目录共 43 个文件（该计数含未推送的内部账本与课程方材料；第三轮推送到公开仓库的为 36 个文件）；引用闭环仍为零未核验、零缺失；`paper.pdf` 当时仍为 16 页、31 条 bibitem（第三轮重编后为 19 页 / 697674 B）。

本轮**未改动论文正文与表格**。理由：全文已在上一轮通过编译与引用双重核验；已知唯一残留是 **3 处 Underfull \hbox**（`parts/05-failure` 表格内 2 处、`parts/06-metrics` 表格内 1 处，均发生在 `tabularx` 单元格断行处，非正文段落——此处更正上一轮"2 处"的记数）。修它必须改表格列定义，会连锁改变分页，使已登记的结构探针页码（图1→p5、表1→p6、图2→p7、表2→p7、表3→p9）全部失效。用"撤销已验证状态"换"消除排版提示"，收益为负，故明确保留不做。

**3. 本轮的新增内容就是这个复核过程本身。**

按提交铁律「提交前 24 小时内有新 commit」，本轮以文档增补构成新 commit，而不是把一周前的产物原样再推一次。

---

## 五、第三轮改写与「指令—输出对照」（2026-09-24）

### 0. 先划一条边界：本文不提供"当时 prompt 的誊写本"

C2 的写作阶段，自然语言指令与模型输出是直接进入本地脚本与编译链执行的，**没有把当时的指令逐字存档**。为回答"关键 prompt 原文"，本轮做了三源检索：

| 检索源 | 方法 | 结果 |
|---|---|---|
| 仓库全量 | 对 `.tex` / `.md` / `pipeline/` / `lit/` 检索指令性字样 | 命中的 "prompt" 全部是参考文献标题（如 *Chain-of-Thought Prompting*）与 rubric 里的"prompt 优化"，无一条是当时的指令原文 |
| 工作区根目录 | 检索指令性开头（"你是/请你/帮我/You are…"） | 无 |
| 对话历史 | 检索 C1 / C2 的指令原文 | 仅 C1 存有指令原文；C2 无 |

结论：「当时 prompt 原文」**不可复原，因此不编造誊写本**。替代方案是给出**可逐字复现的对照**——下表的每一段"输出片段"，都能在本仓库对应文件里逐字找到，或由列出的命令重新产生。

### 1. 本轮指令 → 产出对照（逐字）

**指令（2026-09-24，逐字）**：

> 补C2实测并把核验做成流程硬约束，在AI流程日志中附上关键prompt原文与模型输出片段对照，增强可追溯性，修复Underful/hbox的取舍依据，对L2失败四分法补充分类完备性与互斥性的论证。

| 指令要求 | 产出 | 落盘位置 |
|---|---|---|
| 补 C2 实测 | 缺陷注入 8 臂实验（注入 210 / 拦截 173 / 82.4%） | `parts/07b-experiment.tex`、`out/experiment/defect-injection.{md,json}` |
| 核验做成流程硬约束 | 编译前硬门禁（核验失败 ⇒ 中止编译） | `pipeline/build.py`、`out/experiment/gate-negative-test.txt` |
| 附指令—输出对照 | 本部分 | `AI日志/AI日志.md` 第五节 |
| 修 underfull 取舍依据 | underfull 3 → 0，给出改前改后比对 | `out/experiment/gate-report.json` |
| L2 四分法完备性 / 互斥性 | J1–J4 归类映射 + 互斥性论证 + 完备性论证 | `parts/05-failure.tex` 第 4.3 节 |

### 2. 输出片段对照（逐字，每段均可复现）

**对照 A：引用静态核验基线**

- 命令：`python3 pipeline/check_refs_static.py`（编译前环）
- 输出片段（`out/experiment/check-refs-static.json` 全文）：

```
{
  "entries": 31,
  "cited": 31,
  "findings": []
}
```

- 读数：31 条条目 / 31 个被引 key / 0 条 FAIL 级发现。

**对照 B：硬门禁负向验证（同一命令、只改输入）**

- 输入 A（基线）→ `build.py` 退出码 `0`，报告 `result: PASS`，输出出现编译环 `[2/3]`；
- 输入 B（正文注入 `\cite{zzz_gate_negative_probe_2026}`）→ `build.py` 退出码 `20`，报告 `result: FAIL`，输出**未出现 `[2/3]` 编译环**。
- 输出片段（逐字）：

```
[1/3] 引用核验 -> exit 20 (FAIL)
  - 引用核验未通过（exit 20）：按硬门禁中止，不进入编译
RESULT: FAIL
```

```
  "  [R1] zzz_gate_negative_probe_2026: 正文引用但库中不存在（悬空引用）",
  "RESULT: FAIL"
```

- 读数：核验不是事后检查，而是**流程约束**——坏引用在进入编译环节之前就被拦住。

**对照 C：缺陷注入实验输出（逐字数字）**

| 缺陷类别 | 注入 | 拦截 | 检出率 |
|---|---|---|---|
| D1 悬空引用 | 31 | 31 | 100% |
| D2 幽灵条目 | 31 | 31 | 100% |
| D3a DOI 格式非法 | 12 | 12 | 100% |
| D3b 编造 DOI（格式合法、登记不存在） | 12 | 6 | 50% |
| D4a 标题张冠李戴 | 31 | 31 | 100% |
| D4b 年份漂移 +7 | 31 | 31 | 100% |
| D5 重复 key | 31 | 31 | 100% |
| **D6 语义错配** | 31 | **0** | **0%** |
| **结构类合计** | **210** | **173** | **82.4%** |

基准误报 0 条。D6 是**设计上的盲区**（条目真实、标题年份都对，只是挂在错误的主张上），只能由人工"标题—主张字面比对"拦截；它如实计入分母，不剔除。

**对照 D：编译终态输出（逐字）**

```
[3/3] 产物复核 pages=19 bibitems=31 undefined=0 underfull=0 overfull=1
  - 排版告警：underfull 0、overfull 1（不阻断，但需记录）
RESULT: PASS
```

对应 `out/experiment/gate-report.json` 的 `artifacts`：`pages 19 / bibitems 31 / undefined_refs 0 / underfull_hbox 0 / overfull_hbox 1 / pdf_bytes 697674`。

**对照 E：第一轮留痕的"报错—修法"（时间早于本轮，属可复原的历史记录）**

| 现象（逐字留痕） | 修法 |
|---|---|
| `ValueError: unsupported format character '?' (0x751f)` | 中文"生"紧跟 `%` 被当成格式符 → 改字符串拼接 |
| 相似度 0.75–0.80 被判命中，抽查为"标题像但不是同一篇" | 收紧为只信 `sim=1.00` |
| arXiv 号一度写成 `2205.12950` | 按 `id_list` 返回改回正确编号 |

**引用规范**：本节所有"输出片段"均取自文件原文或命令回显，未改写、未润色；退出码与数字可直接复跑验证。

### 3. 排版取舍依据（更正第四节的"不做"结论）

第四节曾以"修 underfull 会让已登记的结构探针页码全部失效"为理由**明确保留不改**。本轮推翻该理由：

- **前提已变**：本轮按上一轮评测反馈新增第 8 节实测、扩写第 4.3 节，页码**本来就必须重新登记**，"保住旧页码"这个收益已不存在；
- **代价重估**：在必须重编的前提下，改表格列定义只是同一次重编里的顺带动作，边际成本近零；
- **结果**：underfull 3 → **0**；overfull 由 0 → 1（1 处，不阻断，已在 `gate-report.json` 的 `reasons` 中显式记录为"需记录"）。

取舍依据就此更正为：**"该修且已修，改前改后逐项比对并重登记探针"**，而不是"怕页码失效所以不修"。

### 4. 结构探针页码重登记（本轮 live 值）

| 标签 | 编号 | 页 |
|---|---|---|
| `sec:failure` | 4 | 6 |
| `sec:mutex` | 4.3 | 7 |
| `tab:failures` | 1 | 6 |
| `sec:metrics` | 5 | 8 |
| `tab:gradient` | 2 | 8 |
| `fig:gradient` | 2 | 9 |
| `sec:checklist` | 6 | 10 |
| `sec:experiment` | 8 | 12 |
| `tab:injection` | 4 | 13 |

共 23 个标签，`missing: []`、`duplicate_labels: []`。

### 5. 本轮新增的可复跑证据

`out/experiment/` 下新增：`check-refs-static.json`、`gate-report.json`、`gate-negative-test.txt`、`defect-injection.md`、`defect-injection.json`、`page-probe.json`。每条证据由仓库内脚本产生，命令与输入在对应文件内写明，可独立复跑。
