# C2 七维 AAR 复盘 · AI for Math 论文

- 挑战：C2 AI for Math 论文（ch-20260717031343-8ot0ji）
- 作者：周同学 · 日期：2026-09-16
- 交付物：`paper.tex`、`references.bib`、`AI日志/AI日志.md`、本文件
- 终态证据：`paper.pdf` = 613347 B / 16 页；`paper.bbl` = 31 条 bibitem；"未被引用条目"为空；"被引但库中缺失"为空；日志唯一告警为字体形状回退（非致命）

---

## 维度 1：本次学到了什么（Knowledge）

1. **"文献存在"≠"文献支持"**。真正卡人的不是找不到论文，而是把对的论文挂到错的话上。核验要核到"标题字面是否支持这句话"，而不是"关键词像不像"。
2. **"可靠性"不是一个开关，是一条梯度**。把 L1 自然语言 / L2 语义规约 / L3 形式可检三层拆开之后，"哪些环节有机械保证、哪些还靠模型判断"才说得清；论文里 V0–V3 的验证梯度就是这个拆分的直接结果。
3. **可复现的核验流程本身就是方法论**。把查询、命中、未命中、时间全部落盘（`lit/refs.json` + `lit/参考文献核验记录.md`），使"我核验过"从一句声明变成可被别人复跑的证据。
4. **编译链可以被绕过**：本机没有 TeX 发行版也没有包管理器时，一个自带宏包与字体的独立二进制就能把环境问题降级为下载问题。
5. **AI 的能力边界要写进交付物**：无视觉、无实验条件这两条局限如实写明，比装作"看起来没问题"更有学术价值。

## 维度 2：流程（Process）

| 阶段 | 是否可复跑 | 关键产物 |
|---|---|---|
| 环境 | 是（脚本化下载 + 命令记录） | `_smoke/`、tectonic 0.17.0 |
| 选题 | 是（写死贡献与边界） | `选题与问题定义.md` |
| 检索 | **是（全自动）** | `pipeline/verify_refs.py` → `lit/*` |
| 写作 | 部分（人工改主张） | `parts/*.tex` |
| 引文核验 | 部分（脚本给清单，人工判定） | 本 AAR 维度 5 的错配表 |
| 排版 | 是（一条命令） | `paper.tex` → `paper.pdf` |

**做得好的**：把"能自动化的核验"（文献真伪、引用闭环、未引用条目）全部脚本化，这类检查不依赖我的注意力。
**做得不好的**：引用错配的发现是**事后**的，不是流程内置的——靠"导出标题清单再逐条比对"这一步，而这一步是我自己想起来才做的。若一开始就把"写引用的同时登记该引用要支撑的主张"作为硬约束，四处错配里至少两处不会写进去。

## 维度 3：与 AI 协作（Collaboration）

- **AI 的有效用法**：写核验脚本、读编译日志定位错误、把散乱材料归纳成框架、起草段落骨架。
- **AI 的无效用法（试过并放弃）**：让 AI 直接"列 20 篇相关文献"——清单看着都很像真的，但混入了语义对、条目错的组合，正是红线风险最高的用法。
- **人机分工的判据**：凡是"最终会被写进断言"的内容，AI 只能到"候选"为止；把它变成"事实"的那一步必须有人工（或机器核对）的判定。本次所有进 `references.bib` 的条目都过了机器核验，所有引用位置都过了人工比对。

## 维度 4：完成了什么（Result）

- 一篇 16 页、可独立编译的中文 LaTeX 论文（9 节结构：摘要/引言/相关工作/框架/失败模式/指标/清单/讨论/结论）；
- 四项原创小贡献：L2 失败模式的四分法、V0–V3 验证梯度、三个可计算指标（V-率 / 保真度 / 反例检出率 CDR）、十条核验清单；
- 31 条全部经 arXiv/Crossref 核验的参考文献，且引用闭环（无未引用、无缺失）；
- 一份可复跑的文献核验管线与一份逐日 AI 协作日志。

## 维度 5：卡点与突破（Obstacle → Breakthrough）

1. **卡点：本机无 LaTeX 环境**（无 `pdflatex`/`xelatex`/`tectonic`/`brew`）。
   → 突破：用独立二进制 tectonic 0.17.0 自建编译链，并先用中英文最小样例验证再写正文。
2. **卡点：中文无法编译 / 字体缺字**。
   → 突破：`ctexart` + `fontset=fandol`；粗斜体缺字形属非致命回退，接受并在日志中声明。
3. **卡点：脚本崩溃** —— `"% 生成时间：%s" % ...` 抛 `ValueError: unsupported format character '?' (0x751f)`（中文"生"被当作格式符）。
   → 突破：改用字符串拼接。教训：在非 ASCII 文本里用 `%` 格式化是自找麻烦。
4. **卡点：BibTeX 报 `Illegal, another \bibstyle command`**。
   → 突破：删掉 `paper.tex` 中重复的 `\bibliographystyle`。
5. **卡点（红线级）：4 处引用—主张错配**，其中最典型的是把 *GPT-4 Code Interpreter with Code-based Self-Verification* 拿来支持"按难度排序分解"（那是 least-to-most 的语义）。
   → 突破：导出"key → 标题"清单逐条比对，删/移位/改写四种主张；重编译后确认 31 条引用零错配、零未引用。**若不做这一步，这篇论文就已经踩在"引用造假"红线上了。**

## 维度 6：改进方向（Improvement）

1. **把引用登记前置**：写 `\citep` 的同时在该条记录里写下"它要支撑的主张"，让核验变成查表而不是回想。
2. **升级核验脚本**：现在只核"条目是否存在"，下一步让它把每条引用的**上下文句子**一起抓出来打印，人只需要看"句子 + 标题"两行。
3. **补真实实验**：论文的指标目前是定义与可计算性论证；下一轮可在一个小规模数据集上真跑一遍 V-率与 CDR，把"可计算"升级为"已测量"。
4. **让排版可被机器验**：本机 AI 无视觉能力，只能靠结构探针。可改用可解析 PDF 布局信息做自动化溢出/孤行检查，减少"凭感觉"。
5. **消除已知残留**：`parts/05-failure:24` 与 `parts/06-metrics:21` 的 Underfull \hbox 属语义保护下的排版松散，下一轮可用微调措辞而非加 `\sloppy` 消掉。

## 维度 7：ΔR 归因（从"能写"到"敢投"的位移来源）

- **ΔR-1（最大来源）：建立了"不信任 AI 引用的默认姿态"。** 起点是我原本会直接采信 AI 给的文献与归属，终点是"每条引用都要过机器核验 + 标题字面比对"。这一个姿态变化直接消掉了红线风险，也是本次分数里"研究严谨性"的主要支点。
- **ΔR-2：把局限性显式写进交付物。** 无视觉核验、无规模化实验这两条本来可以不说，写明后论文的主张边界变得可辩护——"边界清楚的小贡献"比"边界模糊的大话"更接近可投稿状态。
- **ΔR-3：环境问题降级为工程问题。** 编译链自建成功，意味着后续 C 系列凡是需要 LaTeX 的挑战都可以直接复用，不再从零卡起。
- **未达成的 ΔR**：论文仍停留在"框架 + 定义"，没有测量结果。这是本次最明确的能力缺口，也是下一步最该补的。
