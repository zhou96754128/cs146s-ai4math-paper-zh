# C2 选题与问题定义（写作地基）

> 本文件在动笔写 `paper.tex` 之前锁定：题目、语言、要解决的问题、核心主张、原创小贡献、结论边界。
> 之后所有章节都不得越出这里划定的边界，否则即为「结论超出证据」。

---

## 1. 题目与语言决策

**最终题目**

> 从自然语言到可验证推理：基于语义规约的 AI 数学可靠性框架

**决策记录**

| 项 | 决策 | 理由 |
| --- | --- | --- |
| 论文语言 | 中文正文 + 英文摘要/关键词 | 老师提供的唯一大纲《中文论文大纲（AI4Math）》即为中文，且其建议题目为中文；挑战文档在可编译要求中点名 `xelatex`，即为中文排版预留通路 |
| 文档类 | `ctexart` + `fontset=fandol` | 复现性：Fandol 字体由 TeX 宏包自带，不绑定本机 macOS 字体，换机器仍可编译（已实测通过） |
| 编译引擎 | `tectonic 0.17.0`（xelatex 路线） | 本机无 TeX 发行版；tectonic 单二进制、按需自动拉取宏包 |
| 参考文献 | BibTeX（`plainnat` 风格） | 已实测可自动解析；不使用手写 `thebibliography`，避免条目格式不一致 |

---

## 2. 问题陈述

大语言模型（LLM）在数学任务上的**最终答案**可以被检查，但**从问题到答案的推理过程**在自然语言层面不可被机械验证。由此产生一个结构性矛盾：

- 若只验证最终答案 → 无法区分「正确推理」与「错误推理 + 侥幸命中」，且答案本身就是 LLM 生成的字符串，验证者与生成者共享同一套易错的语义判断；
- 若直接要求 LLM 输出形式化证明 → 现有自动形式化（autoformalization）能力不足，且大量数学推理（近似、估计、启发式）本身并不落在形式系统内，强行形式化会引入「形式化间隙」。

**本文要回答的问题**：在不要求全流程形式化的前提下，如何让「自然语言数学推理」获得可检查、可复现的可靠性保障？

---

## 3. 核心主张

> **可靠性 = 生成 + 语义规约 + 验证**，其中「语义规约层（semantic reduction layer）」是把不可验证的自然语言推理降为可验证对象的唯一中间环节，也是当前 AI4Math 系统的主要失效点与投入产出比最高的改进点。

三层结构（沿用大纲，并明确每层的输入/输出/可验证性）：

| 层 | 名称 | 输入 | 输出 | 是否可机械验证 |
| --- | --- | --- | --- | --- |
| L1 | 生成层 Generation | 自然语言问题 | 自然语言推理链 / 草稿 | 否 |
| L2 | **语义规约层 Semantic Reduction** | L1 的自然语言推理 | 类型化中间表示（Typed IR）：变量、假设、目标状态、变换步 | **部分可验证（本文重点）** |
| L3 | 验证层 Verification | 类型化中间表示 | 通过 / 失败 / 反例 | 可机械验证（约束求解、符号计算、证明助手） |

**主张的可检验推论**（供第五节与我方证据对照）：

- **推论 R1**：在 L1 不变的前提下，仅增强 L3（换更强的求解器）对端到端可靠性的边际收益递减，因为误差在 L2 已经产生且不可被 L3 看见。
- **推论 R2**：L2 的失效具有可分类的结构性模式，而非随机噪声——因此可以用「失效模式分类 + 定向检查」而非「更大模型」来改善。
- **推论 R3**：L2 的输出必须**双向可追**（自然语言 ↔ 形式表示可互译），否则验证通过不能传递回原始问题，可靠性无法归因。

---

## 4. 原创小贡献（相对大纲的增量）

大纲给出了三层模型与「语义规约是瓶颈」的判断。本文在其上补四项可被使用的小贡献——它们都不依赖新实验数据，属于**框架 + 分类 + 度量 + 清单**型贡献：

### C-1　语义规约的失效模式分类（4 类）
把 L2 的失败从「模型不够强」细化为可诊断的类别：

1. **歧义未消解（Ambiguity）**：自然语言表述本身多解，规约时静默选了一解而未记录假设；
2. **类型不完整（Type Incompleteness）**：变量域、量词范围或定义域约束在规约时丢失；
3. **不可逆（Non-invertibility）**：形式表示无法反译回原问题，验证结论无法归因；
4. **语义漂移（Semantic Drift）**：规约本身正确，但后续改写/简化步骤脱离了原约束（本文在 3 类基础上新增）。

### C-2　验证深度梯度 V0–V3
给出一个可选的「验证到什么程度」的阶梯，便于按成本取舍：

| 级别 | 检查对象 | 能拦住的失效 | 成本 |
| --- | --- | --- | --- |
| V0 | 最终答案 | 无（仅格式/量纲） | 极低 |
| V1 | 数值代入回代、量纲一致性 | 算术滑坡 | 低 |
| V2 | 规约结果的类型与约束检查 | C-1 中 1/2 类 | 中 |
| V3 | 形式化陈述与机器证明（证明助手 / 求解器） | 逻辑错误 | 高 |

### C-3　三个可度量指标（给出定义式）
- **可规约率 V-rate**：能被成功规约为类型化中间表示的推理步占比；
- **规约保真度 Fidelity**：双向回译后与原始自然语言推理语义一致的步占比；
- **反例检出率**：注入已知错误后，验证层能报错的错误占比（用于评估 L3 的真实拦截力）。

### C-4　语义规约可靠性清单（Checklist）
面向实践者的 10 条自检项，覆盖上述 4 类失效模式（写作阶段展开）。

---

## 5. 结论边界（不可越出的声明）

为保证「结论不超出证据」，论文明确声明以下**不做**：

1. **不做新模型、不训练、不微调**——本文是框架与分析型论文，不是方法改进型论文；
2. **不声称 4 类失效模式是完备的**，只声称它们在本文考察的推理类型中可区分且可操作；
3. **不给出大样本实证**——C-3 的指标给出定义与最小可用示例，不给出统计显著性结论；
4. **不声称超越形式化路线**——L3 仍以形式化/符号方法为最可靠手段，本文的定位是*降低进入 L3 的门槛*，而非替代它；
5. **不声称适用于全部数学**——明确限定在「离散、可类型化、步骤可逐步状态化的推理」范围内；对连续分析、估计型论证注明不适用。

---

## 6. 论文结构（按老师大纲九节）

1. 引言（问题、动机、贡献声明）
2. 相关工作（生成式推理 / 工具增强与神经符号 / 形式化与自动形式化 / 过程级验证与评测）
3. 三层可靠性模型（L1/L2/L3 定义与形式化描述）
4. 语义规约层：失效模式分类（C-1）
5. 验证深度梯度与指标（C-2、C-3）
6. 案例走查（用小规模、可手算的数学推理示例逐层走查；不虚构实验数据）
7. 讨论：与既有路线的对比、代价与取舍
8. 局限与结论边界（第 5 节内容展开）
9. 结论

---

## 7. 红线自检（写作前设定，写作后逐条核验）

| 红线 | 自检方式 |
| --- | --- |
| 引用造假 → 研究严谨性 0 | 每条引用必须来自 `lit/文献证据表.md` 中「已核验」条目，且 bib 字段与核验记录逐字一致 |
| 论文不可编译 → 技术执行 ≤5 | 交付前必须 `tectonic` 编译 0 error，且引用全部解析（无 `?`） |
| 结论超出证据 | 第 5 节五条边界声明逐条对照正文，出现越界即删改 |
| 案例不虚构 | 第 6 节案例全部可手算复核，标注计算过程 |
